# 台股上班偷看盤

純前端小工具，每 5 秒更新一次台股報價，支援用 `+` 新增股票代號並保存到瀏覽器本地儲存（localStorage）。

## 使用方式

1. 用瀏覽器開啟 `index.html`。
2. 按 `＋` 輸入四位股票代號（例：2330）。
3. 清單會自動更新，並在重開後自動載入先前追蹤清單。

## 備註

- 資料來源：TWSE MIS API。
- 無需資料庫，僅使用 localStorage。
